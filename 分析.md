---

- miaoda-app-chat-sync — ClawHub - ClawHub
- [https://clawhub.ai/xiaoanso/miaoda-app-chat-sync/security/openclaw](https://clawhub.ai/xiaoanso/miaoda-app-chat-sync/security/openclaw)
- Convert Git repository code to structured JSON instructions for AI agents. Fetches code from Git repositories (GitHub, GitLab, etc.), generates structured JS...
- 2026-04-30 21:28

---

基于工件的信息审查，包括 SKILL.md 文件、元数据、安装规范、静态扫描信号和功能信号。ClawScan 不会执行技能或运行运行时探测。

README.md

```
规则：[
    “1. 必须更新文件数组中的所有文件，”
    “2. 必须原样复制内容，”
    3. 必须保留所有空格和格式
  ]
```

该技能有意为下游代理生成命令式指令；这符合目的，但用户应确保生成的 JSON 不会比他们的实际意图更具权威性。

用户影响下游人工智能代理可以将生成的存储库 JSON 视为需要严格遵循的指令。

推荐在将生成的 JSON 交给其他代理之前，请仔细检查，特别是文件列表、操作字段和规则。

scripts/generator.py

```
sync_parser.add_argument('--repo', required=True, help='Git 仓库 URL')
sync_parser.add_argument('--output', help='将输出保存到文件')
```

CLI 接受用户提供的 Git 存储库 URL 和可选的本地输出路径；这是该技能所期望的，但它赋予了该技能网络获取和文件写入行为。

用户影响该技能可以从您指定的存储库中获取代码，并将生成的输出保存到本地。

推荐使用可信的存储库 URL，并仔细选择输出路径。

\_meta.json

```
"ownerId": "kn7cxkk9hebhk933f5kha9kx2582jn66",
"slug": "生成器",
版本：3.0.0
```

内部软件包元数据与注册表元数据命名别名“miaoda-app-chat-sync”版本“3.2.0”不匹配，造成来源和打包歧义。

用户影响验证打包后的产品是否与注册表中的条目和声明的版本相符，难度更大。

推荐安装或使用前，请先确认发布者、软件包标识和版本历史记录，并使用凭据进行验证。

scripts/git/repository.py

```
subprocess.run(['git', 'init'], cwd=target_dir, capture_output=True, timeout=30)
cmd = ['git', '-C', target_dir, 'fetch', 'origin', branch]
```

该技能通过子进程执行本地 Git 命令；这对于获取存储库来说是预期的，并且可见代码不使用 shell 字符串执行。

用户影响该技能会在您的机器或代理环境中运行 Git 操作。

推荐在允许对选定的存储库运行 Git 的环境中使用它。

README.md

```
接收 JSON 指令
    ├─ 解析文件列表和内容
    ├─ 创建/覆盖每个文件
    └─ 输出：已更新的文件列表以供验证
```

预期的下游工作流程可以根据生成的 JSON 创建或覆盖多个文件；错误的存储库、分支、提交或未经审查的输出可能会传播到广泛的代码更改。

用户影响所选存储库或生成的指令中的错误可能会导致下游构建代理覆盖许多文件。

推荐锁定可信提交，在执行前检查文件列表和内容，并保持备份或版本控制回滚可用。

README.md

```
✅ **安全可靠**：自动清理、令牌保护、断路器机制
```

文档提供了广泛的安全保证；用户仍应考虑所需的 GitHub 令牌和下游覆盖工作流程。

用户影响用户可能会过分信任该技能的安全声明，而忽略查看令牌范围或生成的代码更新说明。

推荐将安全声明视为有用的功能，而不是保证；独立审查凭证、输出和下游操作。

元数据

```
必需的环境变量：GITHUB_TOKEN
环境变量声明：无
主凭证：GITHUB_TOKEN
```

该技能需要敏感的 GitHub 凭证，但并未在凭证合约中声明或绑定预期的令牌范围。

用户影响如果提供广泛的 GitHub 令牌，则该技能将获得比读取存储库所需的更大的帐户权限。

推荐使用权限最小、只读的令牌，且该令牌的权限仅限于此任务所需的存储库。

scripts/processors/instruction\_gen.py

```
"规则": prompt_config['规则'],
"content": files_content[filepath]
```

存储库文件内容将直接放入生成的面向代理的 JSON 中，与指令规则一起放置；存储库文本应被视为不受信任的数据。

用户影响存储库文件中的提示性文本可以放置到另一个代理的上下文中。

推荐告诉下游代理将文件内容视为数据，而不是指令，并在使用前检查异常存储库文件。

技能.md

```
此技能（`生成器`）与`miaoda-app-builder`配合使用，采用**两步工作流程**……`miaoda-app-builder`……`通过聊天 API 更新代码`……`创建/覆盖文件`
```

文档中记录的工作流程是将存储库中生成的 JSON 传递给另一个技能/API 以进行代码更新；虽然这一点已公开，但接收者身份、数据处理和权限范围并未完全明确。

用户影响私有存储库代码可以作为预期工作流程的一部分传递给其他代理或聊天 API。

推荐仅将生成的 JSON 发送给受信任的下游代理或服务，并验证其访问控制和数据保留预期。